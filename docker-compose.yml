services:
  db:
    image: mysql:8.0
    container_name: coda-garo-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: coda_garo_DB
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./DB/coda_garo_DB.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db_data: